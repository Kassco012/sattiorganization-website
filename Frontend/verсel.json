{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/((?!api/.*)(?!_next/.*)(?!static/.*)(?!assets/.*).*)",
      "destination": "/index.html"
    }
  ]
}